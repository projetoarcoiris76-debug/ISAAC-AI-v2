# ISAAC AI

Site com entrada protegida por senha e painel do ZAK.

## Rodar
1. Instale Node.js.
2. `npm install`
3. Copie `.env.example` para `.env`.
4. Defina `SITE_PASSWORD` no `.env`.
5. `npm start`
6. Abra `http://localhost:3000`.

Nunca publique `.env`.
